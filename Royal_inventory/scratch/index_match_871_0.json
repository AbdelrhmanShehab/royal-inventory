{
  "name": "run_command",
  "args": {
    "CommandLine": "$code = @\"\nconst fs = require('fs');\nconst appJs = fs.readFileSync('public/js/app.js', 'utf8');\nconst indexHtml = fs.readFileSync('public/index.html', 'utf8');\n\nconst regex = /document\\.getElementById\\(['\"]([^'\"]+)['\"]\\)\\.addEventListener/g;\nlet match;\nwhile ((match = regex.exec(appJs)) !== null) {\n  const id = match[1];\n  if (!indexHtml.includes('id=\"' + id + '\"') && !indexHtml.includes(\"id='\" + id + \"'\")) {\n    console.log('MISSING ID IN HTML:', id);\n  }\n}\n\"@\n[System.IO.File]::WriteAllText('scratch/check_ids.js', $code)\nnode scratch/check_ids.js",
    "Cwd": "e:\\inventory_tracking_system",
    "WaitMsBeforeAsync": 3000,
    "toolAction": "Checking missing IDs",
    "toolSummary": "Checking if there are missing IDs in index.html that cause app.js to crash at the top level."
  }
}